// MATH FUNCTIONS

#include <stdio.h>
#include <math.h>   // This is needed for math functions

int main() {

    float x = 45;
    //x = sqrt(x);
    //x = pow(x, 2);
    //x = round(x);
    //x = ceil(x);
    //x = floor(x);
    //x  = abs(x);
    //x = log(x);
    //x = sin(x);
    //x = cos(x);
    //x = tan(x);


    printf("%.2f", x);

    return 0;

}
